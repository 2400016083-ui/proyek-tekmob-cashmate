name: cashmate_app
description: CashMate - Smart Business Finance Management App
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.11.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  google_fonts: ^6.3.0
  intl: ^0.20.2
  fl_chart: ^1.0.0
  image_picker: ^1.1.2
  google_mlkit_text_recognition: ^0.15.0
  shared_preferences: ^2.5.3
  pdf: ^3.11.1
  printing: ^5.13.4
  firebase_core: ^4.11.0
  firebase_auth: ^6.5.4
  cloud_firestore: ^6.0.0
  google_sign_in: ^7.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/